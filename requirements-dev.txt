# Local development + training + eval requirements.
#
# Installs the slim prod stack plus the heavy bits (torch, transformers,
# xgboost, gradio, pandas, etc.) that are only needed locally and on Colab.
# Railway deploys use requirements.txt and skip all of this.

-r requirements.txt

# Transcription (local whisper fallback; prod uses OpenAI Whisper API)
openai-whisper>=20231117

# Classical ML + NLP features
scikit-learn>=1.4.0
xgboost>=2.0.0
nltk>=3.8.1
vaderSentiment>=3.3.2
textstat>=0.7.4

# Deep learning (DistilBERT fine-tune on Colab)
torch>=2.2.0
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.30.0

# Data handling
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=15.0.0

# Reddit scraping
praw>=7.7.1

# Gradio for gold-set labeling app
gradio>=4.20.0

# Evaluation / stats
scipy>=1.12.0
krippendorff>=0.6.1
matplotlib>=3.8.0
seaborn>=0.13.0

# HuggingFace Hub (model artifact hosting)
huggingface-hub>=0.20.0

# Dev / test
pytest>=8.0.0
httpx>=0.27.0
