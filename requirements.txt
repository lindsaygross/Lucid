# Production / deploy requirements (used inside Dockerfile).
#
# Ships all three models into prod:
#   - naive      (rule-based, stdlib only)
#   - classical  (XGBoost + TF-IDF + handcrafted features)
#   - deep       (fine-tuned DistilBERT pulled from HF Hub on first request)
#
# Torch is installed in a separate Dockerfile layer using the CPU-only
# PyTorch index — NOT listed here — to keep the image small. See Dockerfile.
#
# Router fallback chain: deep -> classical -> naive. Set
# LUCID_PREFERRED_MODEL=deep in Railway to serve DistilBERT.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
anthropic>=0.40.0
openai>=1.40.0
yt-dlp>=2024.8.0
ffmpeg-python>=0.2.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
Pillow>=10.2.0

# Classical model runtime (TF-IDF + handcrafted features -> XGBoost)
numpy>=1.26.0
scikit-learn>=1.4.0
xgboost>=2.0.0
vaderSentiment>=3.3.2
textstat>=0.7.4

# Deep model runtime (torch installed separately in Dockerfile for CPU-only build).
transformers>=4.40.0
huggingface-hub>=0.20.0
