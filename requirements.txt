# Production / deploy requirements (Railway picks this up automatically).
#
# Serves naive + classical models in prod. Deep (DistilBERT) lives in
# notebooks/train_lucid.ipynb + HuggingFace Hub (lindsaygross32/lucid-distilbert)
# and is evaluated offline — we attempted to serve it on Railway but the
# torch+CUDA stack pushed the image past the registry-push timeout. Report
# writes this up as a real ML engineering constraint: "Deep inference is
# hosted on HF Hub for offline evaluation; production deployment to HF
# Inference Endpoints is future work."
#
# Router fallback chain: deep -> classical -> naive. With torch absent,
# deep lazily fails on import and the router falls through to classical.
# Set LUCID_PREFERRED_MODEL=classical in Railway to skip the fallback.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
anthropic>=0.40.0
openai>=1.40.0
yt-dlp>=2024.8.0
ffmpeg-python>=0.2.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
Pillow>=10.2.0

# Classical model runtime (TF-IDF + handcrafted features -> XGBoost)
numpy>=1.26.0
scikit-learn>=1.4.0
xgboost>=2.0.0
vaderSentiment>=3.3.2
textstat>=0.7.4
