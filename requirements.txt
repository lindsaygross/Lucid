# Production / deploy requirements (Railway picks this up automatically).
#
# Slim stack: FastAPI + API clients (Anthropic, OpenAI) + yt-dlp + Pillow.
# Drops the heavy training + eval stack — those live in requirements-dev.txt.
#
# The inference router falls back naive → classical → deep depending on what
# imports successfully. In this slim install only naive loads; classical and
# deep gracefully degrade. Set LUCID_PREFERRED_MODEL=naive to skip the noisy
# fallback-chain log lines in prod.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
anthropic>=0.40.0
openai>=1.40.0
yt-dlp>=2024.8.0
ffmpeg-python>=0.2.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
Pillow>=10.2.0
